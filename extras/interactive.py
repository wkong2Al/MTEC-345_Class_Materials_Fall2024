# %% 
import numpy as np
import matplotlib.pyplot as plt
import scipy.io.wavfile as wav

# VS Code commands (September 2024):
# - ctrl+enter to run a cell in VS Code
# - shift+enter to run a single/selected line(s) in a different interactive env
# - command+shift+p to open the command palette
#   - "install 'code' command in path"
# - use `# %%` to separate "cells"

# %%
# create a 440 hz sine wav
